SET (TARGET_NAME lizard)

ADD_EXECUTABLE (${TARGET_NAME} ../lizard/plugin_factory.cpp)
TARGET_LINK_LIBRARIES (${TARGET_NAME} lizard-common dl)
SET_TARGET_PROPERTIES (${TARGET_NAME} PROPERTIES COMPILE_FLAGS "-DLZ_DLOPEN")
INSTALL (TARGETS ${TARGET_NAME} DESTINATION bin)
