SET (TARGET_NAME lizard-static)

ADD_LIBRARY (${TARGET_NAME} STATIC ../lizard/plugin_factory.cpp)
SET_TARGET_PROPERTIES (${TARGET_NAME} PROPERTIES COMPILE_FLAGS "-DLZ_STATIC")
TARGET_LINK_LIBRARIES (${TARGET_NAME} lizard-common)
INSTALL (TARGETS ${TARGET_NAME} DESTINATION lib)
